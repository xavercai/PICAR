# 函数编号、命名与依赖关系定义文档

## OEE Test Final v9.19 -- 仿草履虫应激机制小车避障进化验证系统

**文档版本**: v1.0
**生成日期**: 2026-08-20
**源文件**: `23e5d329-e481-4821-ae59-fdcd454c69db_OEETestFinal_v9919_Final.ino` (~8665行)
**函数总数**: 约 185+ 个函数/方法，分布在 14 个模块/类中

---

## 一、函数编号索引表

### 约定说明

- 编号格式: `F-{模块缩写}-{序号}`（三位数字，从001递增）
- 层归属: Layer0-Layer6（详见第二章层次架构归类）
- 物理结构即算法标记: ★ = 核心实现函数

### 1.1 Logger 模块 (LOG)

| 编号 | 函数名 | 行号 | 描述 | 输入参数 | 返回值 | 层归属 |
|------|--------|------|------|----------|--------|--------|
| F-LOG-001 | `Logger::init()` | ~4863 | 初始化日志系统 | 无 | void | L6 |
| F-LOG-002 | `Logger::log()` | ~4864 | 输出普通日志文本到串口 | const char* msg | void | L6 |
| F-LOG-003 | `Logger::logf()` | ~4867 | 输出格式化日志（类似printf） | const char* fmt, ... | void | L6 |
| F-LOG-004 | `Logger::hexdump()` | ~4325 | 十六进制格式输出内存内容 | const uint8_t* data, size_t len | void | L6 |
| F-LOG-005 | `Logger::isEnabled()` | ~4340 | 判断日志功能是否启用 | 无 | bool | L6 |
| F-LOG-006 | `Logger::setEnabled()` | ~4355 | 设置日志开关状态 | bool enabled | void | L6 |

**模块统计**: 6个函数

### 1.2 CRC32 模块 (CRC)

| 编号 | 函数名 | 行号 | 描述 | 输入参数 | 返回值 | 层归属 |
|------|--------|------|------|----------|--------|--------|
| F-CRC-001 | `CRC32::update()` | ~4405 | 增量更新CRC32计算状态 | uint8_t byte | void | L6 |
| F-CRC-002 | `CRC32::calculate()` | ~4930 | 计算字节数组的CRC32校验值 | const uint8_t* data, size_t len | uint32_t | L6 |
| F-CRC-003 | `CRC32::verify()` | ~4942 | 验证数据块的CRC32完整性 | const uint8_t* data, size_t len, uint32_t expected | bool | L6 |

**模块统计**: 3个函数

### 1.3 FirmwareVersionManager 模块 (FVM)

| 编号 | 函数名 | 行号 | 描述 | 输入参数 | 返回值 | 层归属 |
|------|--------|------|------|----------|--------|--------|
| F-FVM-001 | `FirmwareVersionManager::isNewVersion()` | ~5247 | 检测当前固件是否为新版本（对比SPIFFS中保存的版本标记） | 无 | bool | L4 |
| F-FVM-002 | `FirmwareVersionManager::cleanAllData()` | ~5258 | 清除旧版本所有实验数据（SPIFFS格式化） | 无 | void | L4 |
| F-FVM-003 | `FirmwareVersionManager::writeVersionMarker()` | ~5277 | 将当前固件版本号写入SPIFFS版本标记文件 | 无 | void | L4 |

**模块统计**: 3个函数

### 1.4 FileUtils 模块 (FLU)

| 编号 | 函数名 | 行号 | 描述 | 输入参数 | 返回值 | 层归属 |
|------|--------|------|------|----------|--------|--------|
| F-FLU-001 | `FileUtils::atomicWrite()` | ~5187 | 原子写入二进制数据（临时文件+重命名，含3次重试） | const String& path, const uint8_t* data, size_t len | bool | L3 |
| F-FLU-002 | `FileUtils::atomicWriteString()` | ~5212 | 原子写入字符串数据 | const String& path, const String& data | bool | L3 |
| F-FLU-003 | `FileUtils::safeRead()` | ~5216 | 安全读取文件内容（含存在性检查） | const String& path | String | L3 |
| F-FLU-004 | `FileUtils::getFileSize()` | ~5226 | 获取SPIFFS文件大小 | const String& path | size_t | L3 |
| F-FLU-005 | `FileUtils::exists()` | ~5236 | 检查SPIFFS文件是否存在 | const String& path | bool | L3 |

**模块统计**: 5个函数

### 1.5 RobustStorage 模块 (ROB)

| 编号 | 函数名 | 行号 | 描述 | 输入参数 | 返回值 | 层归属 |
|------|--------|------|------|----------|--------|--------|
| F-ROB-001 | `RobustStorage::loadHistory()` | ~4964 (private) | 从SPIFFS加载历史记录到RAM缓冲区 | 无 | void | L3 |
| F-ROB-002 | `RobustStorage::init()` | ~5005 | 初始化鲁棒存储系统（挂载SPIFFS、加载历史） | 无 | bool | L3 |
| F-ROB-003 | `RobustStorage::appendToFile()` | ~4964 (private) | 追加数据到SPIFFS文件（私有辅助） | const char* path, const String& data | void | L3 |
| F-ROB-004 | `RobustStorage::addRecord()` | ~5034 | 添加一条进化历史记录到RAM缓冲区 | const HistoryRecord& record | void | L3 |
| F-ROB-005 | `RobustStorage::addChaosRecord()` | ~5104 | 添加一条混沌测试记录到RAM混沌缓冲区 | const ChaoticTestRecord& record | void | L3 |
| F-ROB-006 | `RobustStorage::flushBuffer()` | ~5051 | 将历史记录RAM缓冲区刷新写入SPIFFS | 无 | void | L3 |
| F-ROB-007 | `RobustStorage::flushChaosBuffer()` | ~5127 | 将混沌记录RAM缓冲区刷新写入SPIFFS | 无 | void | L3 |
| F-ROB-008 | `RobustStorage::forceSave()` | ~5068 | 强制保存所有缓冲数据到SPIFFS | 无 | void | L3 |
| F-ROB-009 | `RobustStorage::tick()` | ~5074 | 周期性保存调度（每10秒或满5条触发刷新） ★ | 无 | void | L3 |
| F-ROB-010 | `RobustStorage::tickImpl()` | ~4996 | tick()的内部实现 ★ | 无 | void | L3 |
| F-ROB-011 | `RobustStorage::saveBatchToSPIFFS()` | ~4886 | 批量写入历史记录到SPIFFS文件 | 无 | void | L3 |
| F-ROB-012 | `RobustStorage::clearAll()` | ~5080 | 清除RAM中的所有缓冲数据 | 无 | void | L3 |
| F-ROB-013 | `RobustStorage::getCSVData()` | ~5084 | 获取CSV格式的进化历史数据 | 无 | String | L3 |
| F-ROB-014 | `RobustStorage::getChaosHistoryCSV()` | ~5152 | 获取CSV格式的混沌历史数据 | 无 | String | L3 |
| F-ROB-015 | `RobustStorage::isReady()` | ~5072 | 检查存储系统是否就绪 | 无 | bool | L3 |
| F-ROB-016 | `RobustStorage::getTotalSaved()` | ~5073 | 获取已保存的进化历史记录总数 | 无 | uint32_t | L3 |
| F-ROB-017 | `RobustStorage::getRecordCount()` | ~4632 | 获取当前RAM缓冲区中的记录数 | 无 | uint32_t | L3 |
| F-ROB-018 | `RobustStorage::getChaosRecordCount()` | ~4648 | 获取当前RAM混沌缓冲区中的记录数 | 无 | uint32_t | L3 |
| F-ROB-019 | `RobustStorage::dumpRecords()` | ~4933 | 将所有记录dump输出到串口 | 无 | void | L3 |
| F-ROB-020 | `RobustStorage::getHistoryBuffer()` | ~4964 | 获取完整的历史记录缓冲区引用 | 无 | HistoryRecord* | L3 |
| F-ROB-021 | `RobustStorage::getBufferCapacity()` | ~4980 | 获取历史记录缓冲区的最大容量 | 无 | uint32_t | L3 |
| F-ROB-022 | `RobustStorage::formatSPIFFS()` | ~5161 | 格式化SPIFFS分区 | 无 | bool | L3 |

**模块统计**: 22个函数

### 1.6 Gene 结构体 (GNE)

| 编号 | 函数名 | 行号 | 描述 | 输入参数 | 返回值 | 层归属 |
|------|--------|------|------|----------|--------|--------|
| F-GNE-001 | `Gene::init()` | ~5314 | 随机初始化基因（生成随机行为规则表） | 无 | void | L4 |
| F-GNE-002 | `Gene::evaluateCondition()` | ~5439 | 评估行为规则条件（IF-THEN匹配判断） ★ | int leftSensor, int rightSensor, int condType, int32_t condValue, uint32_t timeMs | bool | L4 |
| F-GNE-003 | `Gene::mutate()` | ~5339 | 基因变异操作（基于物理噪声驱动变异步长） ★ | float mutationRate | void | L4 |
| F-GNE-004 | `Gene::crossover()` | ~5372 | 基因交叉操作（规则子集交换，含混沌规则继承） ★ | const Gene& parent1, const Gene& parent2, Gene& child | void (static) | L4 |

**模块统计**: 4个函数

### 1.7 BehaviorRule 结构体 (BHR)

| 编号 | 函数名 | 行号 | 描述 | 输入参数 | 返回值 | 层归属 |
|------|--------|------|------|----------|--------|--------|
| F-BHR-001 | `BehaviorRule::randomize()` | ~4683 | 随机生成一条行为规则（条件类型、阈值、电机指令） | 无 | void | L4 |
| F-BHR-002 | `BehaviorRule::clamp()` | ~4694 | 将规则参数约束在有效范围内 | 无 | void | L4 |
| F-BHR-003 | `BehaviorRule::compare()` | ~4702 | 比较规则的条件值与实际传感器/状态值 | int sensorLeft, int sensorRight, int condType | bool | L4 |

**模块统计**: 3个函数

### 1.8 BehaviorDescriptor 结构体 (BHD)

| 编号 | 函数名 | 行号 | 描述 | 输入参数 | 返回值 | 层归属 |
|------|--------|------|------|----------|--------|--------|
| F-BHD-001 | `BehaviorDescriptor::init()` | ~4721 | 初始化行为描述符（归零所有维度） | 无 | void | L4 |
| F-BHD-002 | `BehaviorDescriptor::distance()` | ~4728 | 计算两个行为描述符之间的欧氏距离 | const BehaviorDescriptor& other | float | L4 |
| F-BHD-003 | `BehaviorDescriptor::normalize()` | ~4745 | 使用档案最大值对描述符做归一化 | const BehaviorDescriptor& maxValues | void | L4 |

**模块统计**: 3个函数

### 1.9 SensorCalibration 模块 (SCL)

| 编号 | 函数名 | 行号 | 描述 | 输入参数 | 返回值 | 层归属 |
|------|--------|------|------|----------|--------|--------|
| F-SCL-001 | `SensorCalibration::calibrate()` | ~4887 | 执行传感器零点校准（读取无遮挡环境基准值并减去） | 无 | void | L1 |
| F-SCL-002 | `SensorCalibration::setThresholds()` | ~4904 | 设置左右传感器的障碍物阈值 | int16_t leftThreshold, int16_t rightThreshold | void | L1 |
| F-SCL-003 | `SensorCalibration::getLeftRaw()` | ~4909 | 获取左传感器原始ADC读数 | 无 | int16_t | L1 |
| F-SCL-004 | `SensorCalibration::getRightRaw()` | ~4910 | 获取右传感器原始ADC读数 | 无 | int16_t | L1 |
| F-SCL-005 | `SensorCalibration::isObstacleLeft()` | ~4911 | 判断左传感器是否检测到障碍物 | 无 | bool | L1 |
| F-SCL-006 | `SensorCalibration::isObstacleRight()` | ~4912 | 判断右传感器是否检测到障碍物 | 无 | bool | L1 |
| F-SCL-007 | `SensorCalibration::isClear()` | ~4913 | 判断左右传感器是否都无遮挡 | 无 | bool | L1 |
| F-SCL-008 | `SensorCalibration::readNoise()` | ~4917 | 读取悬空ADC引脚热噪声值（物理真随机源） ★ | 无 | uint16_t | L1 |

**模块统计**: 8个函数

### 1.10 MotorController 模块 (MC)

| 编号 | 函数名 | 行号 | 描述 | 输入参数 | 返回值 | 层归属 |
|------|--------|------|------|----------|--------|--------|
| F-MC-001 | `MotorController::flushChaosSnapshot()` | ~6452 (private) | 冲刷当前混沌快照段到缓冲区 | 无 | void | L3 |
| F-MC-002 | `MotorController::saveChaosSnapshotsToSPIFFS()` | ~6461 (private) | 紧急将混沌快照缓冲区持久化到SPIFFS | 无 | void | L3 |
| F-MC-003 | `MotorController::clearChaosSnapshots()` | ~6475 (private) | 清空混沌快照内存缓冲区 | 无 | void | L3 |
| F-MC-004 | `MotorController::setMotorSpeed()` | ~6483 (private) ★ | 设置电机PWM速度（含死区补偿、分方向增益、方向控制） | int16_t leftPWM, int16_t rightPWM | void | L1 |
| F-MC-005 | `MotorController::updateEncoders()` | ~6544 (private) ★ | 更新编码器读数（读取中断计数器差值） | 无 | void | L1 |
| F-MC-006 | `MotorController::clearStuckState()` | ~6562 (private) | 清除卡死检测状态（重置堵转帧计数器和标志） | 无 | void | L1 |
| F-MC-007 | `MotorController::setMotorState()` | ~6572 (private) ★ | 设置电机状态机（3bit状态枚举：IDLE/FORWARD/TURN/REVERSE/CHAOS/DEAD等） | uint8_t newState | void | L2 |
| F-MC-008 | `MotorController::detectStuckWithWindow()` | ~6581 (private) ★ | 滑动窗口卡死检测（连续多帧编码器无变化=物理停止） | 无 | bool | L2 |
| F-MC-009 | `MotorController::isStuck()` | ~6600 | 判断电机是否处于卡死状态 | 无 | bool | L2 |
| F-MC-010 | `MotorController::isChaosActive()` | ~6601 | 判断混沌模式是否激活 | 无 | bool | L2 |
| F-MC-011 | `MotorController::isDead()` | ~6602 | 判断个体是否死亡（碰撞终止） | 无 | bool | L2 |
| F-MC-012 | `MotorController::getMotorState()` | ~6603 | 获取当前电机状态枚举值 | 无 | uint8_t | L2 |
| F-MC-013 | `MotorController::getStateDuration()` | ~6604 | 获取当前状态已持续的时间（毫秒） | 无 | uint32_t | L2 |
| F-MC-014 | `MotorController::getObstacleThreshold()` | ~6606 | 获取障碍物检测阈值 | 无 | int16_t | L1 |
| F-MC-015 | `MotorController::getClearThreshold()` | ~6607 | 获取道路畅通阈值 | 无 | int16_t | L1 |
| F-MC-016 | `MotorController::getChaosNoiseAmplifier()` | ~6608 | 获取混沌噪声放大系数 | 无 | float | L2 |
| F-MC-017 | `MotorController::getChaosMinPwm()` | ~6609 | 获取混沌模式最小PWM值 | 无 | int16_t | L2 |
| F-MC-018 | `MotorController::getChaosTimeoutMs()` | ~6610 | 获取混沌超时阈值（毫秒） | 无 | uint32_t | L2 |
| F-MC-019 | `MotorController::syncGeneParams()` | ~6612 ★ | 同步基因参数到电机控制器（11个物理参数从基因同步到控制器） | const Gene& gene | void | L4 |
| F-MC-020 | `MotorController::getTotalStuckTime()` | ~6628 | 获取累计卡死总时长 | 无 | uint32_t | L2 |
| F-MC-021 | `MotorController::getStateDistanceDelta()` | ~6633 | 获取当前状态内的距离增量（编码器脉冲差） | 无 | int32_t | L2 |
| F-MC-022 | `MotorController::getChaosDuration()` | ~6634 | 获取当前混沌模式已持续时间 | 无 | uint32_t | L2 |
| F-MC-023 | `MotorController::getTestChaosTriggerCount()` | ~6639 | 获取测试中混沌触发次数统计 | 无 | uint32_t | L5 |
| F-MC-024 | `MotorController::getChaosSnapshotCount()` | ~6663 | 获取混沌快照缓冲区中的快照数量 | 无 | uint32_t | L3 |
| F-MC-025 | `MotorController::getChaosSnapshots()` | ~6664 | 获取混沌快照缓冲区引用 | 无 | ChaosSnapshot* | L3 |
| F-MC-026 | `MotorController::resetChaosStatistics()` | ~6666 | 重置混沌统计（触发次数、时长等归零） | 无 | void | L5 |
| F-MC-027 | `MotorController::resetStuck()` | ~6689 | 重置卡死状态（用于下一个个体的开始） | 无 | void | L2 |
| F-MC-028 | `MotorController::getLeftTicks()` | ~6693 | 获取左轮编码器累计脉冲数 | 无 | int32_t | L1 |
| F-MC-029 | `MotorController::getRightTicks()` | ~6694 | 获取右轮编码器累计脉冲数 | 无 | int32_t | L1 |
| F-MC-030 | `MotorController::getEncoderDiff()` | ~6695 | 获取左右编码器脉冲差值 | 无 | int32_t | L1 |
| F-MC-031 | `MotorController::getDistanceTicks()` | ~7147 | 获取累计行驶距离（编码器脉冲总数） | 无 | int32_t | L1 |
| F-MC-032 | `MotorController::resetDistanceTicks()` | ~7148 | 重置累计行驶距离为零 | 无 | void | L1 |
| F-MC-033 | `MotorController::getLeftSensor()` | ~6696 | 获取左传感器最近一次读数 | 无 | int16_t | L1 |
| F-MC-034 | `MotorController::getRightSensor()` | ~6697 | 获取右传感器最近一次读数 | 无 | int16_t | L1 |
| F-MC-035 | `MotorController::init()` | ~6702 | 完整初始化电机控制器（GPIO/LEDC/PWM/编码器中断/初始状态） | 无 | void | L1 |
| F-MC-036 | `MotorController::stopMotors()` | ~6785 | 停止所有电机（PWM归零） | 无 | void | L1 |
| F-MC-037 | `MotorController::enableMotor()` | ~6794 | 启用电机输出（解除锁定） | 无 | void | L1 |
| F-MC-038 | `MotorController::disableMotor()` | ~6795 | 禁用电机输出（锁定安全） | 无 | void | L1 |
| F-MC-039 | `MotorController::isMotorEnabled()` | ~6796 | 检查电机是否已启用 | 无 | bool | L1 |
| F-MC-040 | `MotorController::startPhysicsAction()` | ~6798 ★ | 开始物理动作（非阻塞，设置电机并记录起始状态） | int16_t leftPWM, int16_t rightPWM, uint16_t minDurationMs | void | L2 |
| F-MC-041 | `MotorController::updatePhysicsAction()` | ~6812 ★ | 更新物理动作状态（每帧检测编码器增量，物理停止自动终止） | 无 | void | L2 |
| F-MC-042 | `MotorController::forceStopAction()` | ~6822 | 强制停止当前动作 | 无 | void | L2 |
| F-MC-043 | `MotorController::isActionInProgress()` | ~6829 | 判断是否有动作正在执行 | 无 | bool | L2 |
| F-MC-044 | `MotorController::startChaos()` | ~6831 ★ | 启动混沌模式（激活噪声驱动、记录起始快照） | 无 | void | L2 |
| F-MC-045 | `MotorController::updateChaos()` | ~6868 ★ | 更新混沌模式（噪声连续调制PWM、快照收集、恢复检测） | 无 | void | L2 |
| F-MC-046 | `MotorController::endChaos()` | ~6954 ★ | 结束混沌模式（记录终止距离、冲刷快照、恢复状态机） | 无 | void | L2 |
| F-MC-047 | `MotorController::startFrameLog()` | ~6978 | 开始帧日志记录会话 | uint8_t generation, uint8_t individual | void | L2 |
| F-MC-048 | `MotorController::logFrame()` | ~6986 | 记录单帧数据（传感器、电机PWM、方向标志、混沌标志） | 无 | void | L2 |
| F-MC-049 | `MotorController::update()` | ~7025 ★ | 主更新循环（基因驱动、卡死检测、混沌触发、帧日志、状态机核心） | 无 | void | L2 |
| F-MC-050 | `MotorController::getFrameLog()` | ~7150 | 获取帧日志缓冲区引用 | 无 | FrameLogEntry* | L2 |

**模块统计**: 50个函数（含getter系列函数的子项展开计数）

### 1.11 RollingStorage 模块 (ROL)

| 编号 | 函数名 | 行号 | 描述 | 输入参数 | 返回值 | 层归属 |
|------|--------|------|------|----------|--------|--------|
| F-ROL-001 | `RollingStorage::getFreeSpace()` | ~5467 (private) | 获取SPIFFS可用空间 | 无 | uint32_t | L3 |
| F-ROL-002 | `RollingStorage::getStoredGenerations()` | ~5481 (private) | 获取SPIFFS中已存储的代数列表 | 无 | std::vector<uint8_t> | L3 |
| F-ROL-003 | `RollingStorage::deleteGeneration()` | ~5504 (private) | 删除指定代的所有SPIFFS文件 | uint8_t generation | void | L3 |
| F-ROL-004 | `RollingStorage::deleteOldestGeneration()` | ~5237 | 删除最早代的SPIFFS数据（滚动覆盖） | 无 | void | L3 |
| F-ROL-005 | `RollingStorage::generateManifest()` | ~5100 | 生成SPIFFS中所有实验文件清单字符串 | 无 | String | L3 |
| F-ROL-006 | `RollingStorage::generateAllDataReport()` | ~5133 | 生成全量数据报告（聚合所有代的所有数据） | 无 | String | L3 |
| F-ROL-007 | `RollingStorage::exportAllToSD()` | ~5175 | 导出所有SPIFFS数据到SD卡 | 无 | bool | L3 |
| F-ROL-008 | `RollingStorage::clearAllData()` | ~5208 | 清除SPIFFS中所有滚动存储数据 | 无 | void | L3 |
| F-ROL-009 | `RollingStorage::ensureChipID()` | ~5262 | 确保设备唯一ID文件存在 | 无 | void | L3 |
| F-ROL-010 | `RollingStorage::getChipID()` | ~5284 | 获取设备唯一标识符 | 无 | String | L3 |
| F-ROL-011 | `RollingStorage::init()` | ~5301 | 初始化滚动存储（加载已有代数、空间管理） | 无 | void | L3 |
| F-ROL-012 | `RollingStorage::ensureSpace()` | ~5347 | 确保SPIFFS有足够空间（不足时删除最旧代） | uint32_t requiredBytes | bool | L3 |
| F-ROL-013 | `RollingStorage::saveFrameLog()` | ~5558 | 保存帧日志到SPIFFS（二进制压缩格式，含文件头+CRC32） | uint8_t generation, uint8_t individual, FrameLogEntry* frames, uint16_t frameCount | bool | L3 |
| F-ROL-014 | `RollingStorage::incrementalSave()` | ~5647 | 增量保存帧日志（每500帧触发一次，减少IO开销） | uint8_t generation, uint8_t individual, FrameLogEntry* frames, uint16_t frameCount | bool | L3 |
| F-ROL-015 | `RollingStorage::resetGenerationCounter()` | ~5655 | 重置代际计数器 | 无 | void | L3 |
| F-ROL-016 | `RollingStorage::getStorageStatus()` | ~5659 | 获取存储系统状态信息字符串 | 无 | String | L3 |
| F-ROL-017 | `RollingStorage::getFreeSpaceKB()` | ~5671 | 获取SPIFFS可用空间（KB） | 无 | uint32_t | L3 |

**模块统计**: 17个函数

### 1.12 GeneStorage 模块 (GST)

| 编号 | 函数名 | 行号 | 描述 | 输入参数 | 返回值 | 层归属 |
|------|--------|------|------|----------|--------|--------|
| F-GST-001 | `GeneStorage::init()` | ~5696 | 初始化基因存储（加载实验状态、准备目录） | 无 | void | L3 |
| F-GST-002 | `GeneStorage::forceActivate()` | ~5709 | 强制激活实验（无视状态标记） | 无 | void | L3 |
| F-GST-003 | `GeneStorage::startNewExperiment()` | ~5717 | 开始新实验（创建实验ID、初始化代数计数） | 无 | void | L3 |
| F-GST-004 | `GeneStorage::setCurrentGeneration()` | ~5730 | 设置当前进化代数 | uint8_t generation | void | L3 |
| F-GST-005 | `GeneStorage::shouldStartNewExperiment()` | ~5736 | 判断是否需要开始新的20代实验周期 | 无 | bool | L3 |
| F-GST-006 | `GeneStorage::isExperimentActive()` | ~5770 | 判断实验是否处于活跃状态 | 无 | bool | L3 |
| F-GST-007 | `GeneStorage::getCurrentGeneration()` | ~5771 | 获取当前进化代数 | 无 | uint8_t | L3 |
| F-GST-008 | `GeneStorage::getMaxGenerations()` | ~5772 | 获取固定实验周期最大代数（20代） | 无 | uint8_t | L3 |
| F-GST-009 | `GeneStorage::getProgress()` | ~5773 | 获取实验进度（百分比） | 无 | float | L3 |
| F-GST-010 | `GeneStorage::isExperimentComplete()` | ~5774 | 判断实验是否已完成（达到20代） | 无 | bool | L3 |
| F-GST-011 | `GeneStorage::incrementGeneration()` | ~5775 | 递增当前代数（代数+1并持久化状态） | 无 | void | L3 |
| F-GST-012 | `GeneStorage::savePopulation()` | ~5781 | 保存种群快照到SPIFFS（二进制格式，含CRC32校验） | uint8_t generation, const Individual* pop, uint8_t popSize | bool | L3 |
| F-GST-013 | `GeneStorage::loadPopulation()` | ~5840 | 从SPIFFS加载种群快照（二进制格式，含CRC32校验和版本检查） | uint8_t generation, Individual* pop, uint8_t& popSize | bool | L3 |
| F-GST-014 | `GeneStorage::saveIndividualRecord()` | ~5942 | 保存个体基因记录到CSV（含混沌规则标记） | uint8_t generation, uint8_t individual, const Gene& gene, float novelty, uint32_t survivalMs, int32_t distanceTicks, uint8_t ruleCount, bool hasChaosRules | void | L3 |
| F-GST-015 | `GeneStorage::saveChaosRecord()` | ~5988 | 保存混沌测试记录到CSV | uint8_t generation, uint8_t individual, const ChaoticTestRecord& record | void | L3 |
| F-GST-016 | `GeneStorage::populationFileExists()` | ~6024 | 检查指定代的种群快照文件是否存在 | uint8_t generation | bool | L3 |
| F-GST-017 | `GeneStorage::clearAllExperimentData()` | ~6030 | 清除所有实验数据（SPIFFS文件+RAM状态） | 无 | void | L3 |
| F-GST-018 | `GeneStorage::saveExperimentState()` | ~6053 (private) | 保存实验状态标记到SPIFFS（experiment_state.mrk） | 无 | void | L3 |
| F-GST-019 | `GeneStorage::loadExperimentState()` | ~6060 (private) | 从SPIFFS加载实验状态标记 | 无 | bool | L3 |

**模块统计**: 19个函数

### 1.13 NoveltyArchive 模块 (NOV)

| 编号 | 函数名 | 行号 | 描述 | 输入参数 | 返回值 | 层归属 |
|------|--------|------|------|----------|--------|--------|
| F-NOV-001 | `NoveltyArchive::updateMaxValues()` | ~6111 (private) | 更新行为描述符各维度历史最大值 | const BehaviorDescriptor& bd | void | L4 |
| F-NOV-002 | `NoveltyArchive::init()` | ~6121 | 初始化新奇度档案（清空环形缓冲区） | 无 | void | L4 |
| F-NOV-003 | `NoveltyArchive::computeNovelty()` | ~6123 ★ | 计算个体行为的新奇度得分（k近邻平均欧氏距离） | const BehaviorDescriptor& bd | float | L4 |
| F-NOV-004 | `NoveltyArchive::addIfNovel()` | ~6145 ★ | 若行为新奇度超过阈值，则将描述符加入档案 | const BehaviorDescriptor& bd | bool | L4 |
| F-NOV-005 | `NoveltyArchive::extractFromFrameLog()` | ~6160 ★ | 从帧日志中提取12维行为描述符（传感器/运动/策略三组维度） | const FrameLogEntry* frames, uint16_t frameCount, bool excludeChaosFrames | BehaviorDescriptor | L4 |
| F-NOV-006 | `NoveltyArchive::getArchiveSize()` | ~6214 | 获取档案中已存储的行为描述符数量 | 无 | uint16_t | L4 |

**模块统计**: 6个函数

### 1.14 RAMLogBuffer 模块 (RLB)

| 编号 | 函数名 | 行号 | 描述 | 输入参数 | 返回值 | 层归属 |
|------|--------|------|------|----------|--------|--------|
| F-RLB-001 | `RAMLogBuffer::compressFrame()` | ~6230 (private) | 压缩帧数据（差分编码：PWM差值替代绝对值） | const FrameLogEntry& frame | CompressedFrameEntry | L2 |
| F-RLB-002 | `RAMLogBuffer::init()` | ~6253 | 初始化RAM日志缓冲区（6000帧环形缓冲） | 无 | void | L2 |
| F-RLB-003 | `RAMLogBuffer::startNewTest()` | ~6264 | 开始新测试（记录代/个体编号，清空缓冲区） | uint8_t generation, uint8_t individual | void | L2 |
| F-RLB-004 | `RAMLogBuffer::addFrame()` | ~6273 ★ | 添加帧到环形缓冲区（含压缩存储） | const FrameLogEntry& frame | void | L2 |
| F-RLB-005 | `RAMLogBuffer::extractFrames()` | ~6290 | 提取帧数据（文本格式，用于串口输出） | uint16_t start, uint16_t count | String | L5 |
| F-RLB-006 | `RAMLogBuffer::extractBinary()` | ~6335 | 提取帧数据（二进制格式，用于SPIFFS写入） | uint16_t start, uint16_t count | uint8_t* | L5 |
| F-RLB-007 | `RAMLogBuffer::getFrameCount()` | ~6356 | 获取缓冲区中已存储的帧数量 | 无 | uint16_t | L2 |
| F-RLB-008 | `RAMLogBuffer::getGeneration()` | ~6357 | 获取当前测试的代数编号 | 无 | uint8_t | L2 |
| F-RLB-009 | `RAMLogBuffer::getIndividual()` | ~6358 | 获取当前测试的个体编号 | 无 | uint8_t | L2 |
| F-RLB-010 | `RAMLogBuffer::hasDataAvailable()` | ~6359 | 判断缓冲区是否有可用数据 | 无 | bool | L2 |
| F-RLB-011 | `RAMLogBuffer::clear()` | ~6360 | 清空缓冲区（重置读写指针） | 无 | void | L2 |

**模块统计**: 11个函数

### 1.15 EvolutionEngine 模块 (EVE)

| 编号 | 函数名 | 行号 | 描述 | 输入参数 | 返回值 | 层归属 |
|------|--------|------|------|----------|--------|--------|
| F-EVE-001 | `EvolutionEngine::initImpl()` | ~7269 (private) | 初始化进化引擎实现（恢复种群或创建初始种群） | 无 | void | L4 |
| F-EVE-002 | `EvolutionEngine::startCurrentTestImpl()` | ~7317 (private) ★ | 开始当前个体测试实现（同步基因参数、启动帧日志） | 无 | void | L4 |
| F-EVE-003 | `EvolutionEngine::endCurrentTestImpl()` | ~7334 (private) ★ | 结束当前个体测试实现（混沌注入、新奇度评估、记录保存） | 无 | void | L4 |
| F-EVE-004 | `EvolutionEngine::nextIndividualImpl()` | ~7477 (private) ★ | 进入下一个个体实现（判定是否触发代际进化） | 无 | void | L4 |
| F-EVE-005 | `EvolutionEngine::inferRuleFromSnapshot()` | ~7494 (static) ★ | 从混沌快照反推行为规则（物理经验基因化） | const ChaosSnapshot& snapshot, const Gene& currentGene, Gene& outGene | void | L4 |
| F-EVE-006 | `EvolutionEngine::evolveImpl()` | ~7536 (private) ★ | 执行代际进化（按新奇度排序+精英保留+交叉+变异） | 无 | void | L4 |
| F-EVE-007 | `EvolutionEngine::tournamentSelectByNovelty()` | ~7586 (private) | 基于新奇度得分的锦标赛选择（规模3） | 无 | uint8_t | L4 |
| F-EVE-008 | `EvolutionEngine::getAvgNovelty()` | ~7595 (private) | 计算当前种群平均新奇度得分 | 无 | float | L4 |
| F-EVE-009 | `EvolutionEngine::init()` | ~7602 | 初始化进化引擎入口（单例代理，调用initImpl） | 无 | void | L4 |
| F-EVE-010 | `EvolutionEngine::startCurrentTest()` | ~7603 | 开始当前测试入口（调用startCurrentTestImpl） | 无 | void | L4 |
| F-EVE-011 | `EvolutionEngine::endCurrentTest()` | ~7604 | 结束当前测试入口（调用endCurrentTestImpl） | 无 | void | L4 |
| F-EVE-012 | `EvolutionEngine::nextIndividual()` | ~7605 | 下一个个体入口（调用nextIndividualImpl） | 无 | void | L4 |
| F-EVE-013 | `EvolutionEngine::isTestActive()` | ~7606 | 判断当前是否有活跃的个体测试 | 无 | bool | L4 |
| F-EVE-014 | `EvolutionEngine::getTestStartTime()` | ~7607 | 获取当前测试开始时间 | 无 | uint32_t | L4 |
| F-EVE-015 | `EvolutionEngine::getGeneration()` | ~7608 | 获取当前进化代数 | 无 | uint8_t | L4 |
| F-EVE-016 | `EvolutionEngine::getIndividual()` | ~7609 | 获取当前个体索引 | 无 | uint8_t | L4 |
| F-EVE-017 | `EvolutionEngine::getMutationRate()` | ~7610 | 获取当前变异率 | 无 | float | L4 |
| F-EVE-018 | `EvolutionEngine::getBestNoveltyEver()` | ~7611 | 获取历史最佳新奇度得分 | 无 | float | L4 |
| F-EVE-019 | `EvolutionEngine::getArchiveSize()` | ~7612 | 获取新奇度档案当前大小 | 无 | uint16_t | L4 |
| F-EVE-020 | `EvolutionEngine::getCurrentGene()` | ~7613 | 获取当前测试个体的基因 | 无 | Gene& | L4 |
| F-EVE-021 | `EvolutionEngine::setPendingTransition()` | ~7614 | 设置待处理的进化状态转换标志 | uint8_t transition | void | L4 |
| F-EVE-022 | `EvolutionEngine::getPendingTransition()` | ~7615 | 获取待处理的进化状态转换标志 | 无 | uint8_t | L4 |
| F-EVE-023 | `EvolutionEngine::setMutationRate()` | ~7617 | 设置变异率（动态调整：0.05-0.50） | float rate | void | L4 |
| F-EVE-024 | `EvolutionEngine::setControlMode()` | ~7618 | 设置控制模式（手动/自动/进化） | uint8_t mode | void | L4 |
| F-EVE-025 | `EvolutionEngine::resetToGeneration1()` | ~7622 | 重置进化到第1代（清空档案和种群） | 无 | void | L4 |

**模块统计**: 25个函数

### 1.16 ChunkedStreamWriter 模块 (CSW)

| 编号 | 函数名 | 行号 | 描述 | 输入参数 | 返回值 | 层归属 |
|------|--------|------|------|----------|--------|--------|
| F-CSW-001 | `ChunkedStreamWriter::sendFileContent()` | ~7646 | 流式发送文本文件内容（HTTP chunked） | WiFiClient& client, const String& path, const String& contentType | void | L5 |
| F-CSW-002 | `ChunkedStreamWriter::sendBinaryFileContent()` | ~7661 | 流式发送二进制文件内容（HTTP chunked） | WiFiClient& client, const String& path | void | L5 |
| F-CSW-003 | `ChunkedStreamWriter::streamMatchingFiles()` | ~7678 | 遍历SPIFFS并流式发送所有匹配模式的文件 | WiFiClient& client, const String& pattern, bool isBinary | void | L5 |

**模块统计**: 3个函数

### 1.17 CarWebServer 模块 (CWS)

| 编号 | 函数名 | 行号 | 描述 | 输入参数 | 返回值 | 层归属 |
|------|--------|------|------|----------|--------|--------|
| F-CWS-001 | `CarWebServer::buildHTMLPage()` | ~7744 (private) | 构建Web管理页面HTML（v99风格仪表盘） | 无 | String | L5 |
| F-CWS-002 | `CarWebServer::init()` | ~7911 | 初始化Web服务器（注册路由+启动HTTP服务） | 无 | void | L5 |
| F-CWS-003 | `CarWebServer::handleClient()` | ~7932 | 处理HTTP客户端请求（分发到对应handler） | 无 | void | L5 |
| F-CWS-004 | `CarWebServer::handleStatus()` | ~7935 (static) | 处理/status API请求（返回JSON格式系统状态） | 无 | void | L5 |
| F-CWS-005 | `CarWebServer::handleEvolution()` | ~7979 (static) | 处理/evolution API请求（start/stop/next进化控制） | 无 | void | L5 |
| F-CWS-006 | `CarWebServer::handleMotor()` | ~8001 (static) | 处理/motor API请求（enable/disable/方向控制） | 无 | void | L5 |
| F-CWS-007 | `CarWebServer::handleAdminReset()` | ~8016 (static) | 处理/admin/reset API请求（系统复位） | 无 | void | L5 |
| F-CWS-008 | `CarWebServer::handleDownloadHistory()` | ~8024 (static) | 处理下载历史记录CSV的HTTP请求 | 无 | void | L5 |
| F-CWS-009 | `CarWebServer::handleDownloadChaos()` | ~8032 (static) | 处理下载混沌测试记录（chunked流式传输） | 无 | void | L5 |
| F-CWS-010 | `CarWebServer::handleDownloadPopulation()` | ~8044 (static) | 处理下载种群快照（chunked流式传输） | 无 | void | L5 |
| F-CWS-011 | `CarWebServer::handleDownloadGenes()` | ~8056 (static) | 处理下载基因记录（chunked流式传输） | 无 | void | L5 |
| F-CWS-012 | `CarWebServer::handleDownloadFrames()` | ~8069 (static) | 处理下载帧日志（chunked流式传输） | 无 | void | L5 |
| F-CWS-013 | `CarWebServer::handleDownloadAll()` | ~8083 (static) | 一键下载所有实验文件（chunked流式传输） | 无 | void | L5 |
| F-CWS-014 | `CarWebServer::handleListFiles()` | ~8151 (static) | 列出SPIFFS中所有文件（JSON格式） | 无 | void | L5 |
| F-CWS-015 | `CarWebServer::handleDownloadFrame()` | ~8168 (static) | 下载指定代和个体的单个帧日志文件 | 无 | void | L5 |
| F-CWS-016 | `CarWebServer::handleDownloadPop()` | ~8192 (static) | 下载指定代的单个种群快照文件 | 无 | void | L5 |

**模块统计**: 16个函数

### 1.18 全局函数 (GLB)

| 编号 | 函数名 | 行号 | 描述 | 输入参数 | 返回值 | 层归属 |
|------|--------|------|------|----------|--------|--------|
| F-GLB-001 | `initWiFi()` | ~8237 | WiFi AP初始化（建立热点） | 无 | bool | L5 |
| F-GLB-002 | `setup()` | ~8252 | Arduino系统初始化入口（依次初始化所有模块） | 无 | void | L6 |
| F-GLB-003 | `loop()` | ~8313 ★ | Arduino主循环（10ms帧循环+串口命令解析+进化状态推进） | 无 | void | L2 |
| F-GLB-004 | `listSPIFFSFiles()` | ~8440 | 列出SPIFFS中所有文件（串口命令） | 无 | void | L5 |
| F-GLB-005 | `printStatus()` | ~8473 | 打印系统状态摘要（串口命令） | 无 | void | L5 |
| F-GLB-006 | `parseFrameLog()` | ~8494 | 解析帧日志文件并输出到串口 | uint8_t generation, uint8_t individual | void | L5 |
| F-GLB-007 | `parsePopulation()` | ~8536 | 解析种群快照文件并输出到串口 | uint8_t generation | void | L5 |
| F-GLB-008 | `parseChaosRecord()` | ~8575 | 解析混沌测试记录并输出到串口 | uint8_t generation, uint8_t individual | void | L5 |
| F-GLB-009 | `parseAllIndividuals()` | ~8588 | 汇总并解析一代中所有个体数据 | uint8_t generation | void | L5 |
| F-GLB-010 | `extractRamLog()` | ~8659 | 提取RAM日志缓冲区内容（串口命令） | uint16_t start, uint16_t end | void | L5 |
| F-GLB-011 | `showStorageStatus()` | ~8663 | 显示存储系统状态（串口命令） | 无 | void | L5 |

**模块统计**: 11个函数

---

## 二、层次架构归类

本系统采用六层架构模型，与论文"三重算法层次+实验基础设施层"的扩展框架相对应：

```
Layer6 (工具层)          - Logger, 全局入口(setup)
Layer5 (接口层)          - CarWebServer, ChunkedStreamWriter, 串口命令函数
Layer4 (业务层/元算法层)  - EvolutionEngine, NoveltyArchive, Gene, GeneStorage, FirmwareVersionManager
Layer3 (持久层/基础设施层) - RobustStorage, RollingStorage, FileUtils, RAMLogBuffer中的持久化接口
Layer2 (缓存层/具身算法层) - MotorController (update/logFrame/startChaos/updateChaos/endChaos), RAMLogBuffer(addFrame), loop()
Layer1 (驱动层)           - MotorController (init/setMotorSpeed/updateEncoders), SensorCalibration
Layer0 (硬件层)           - 编码器ISR(IRAM_ATTR), ADC引脚读取, GPIO读写, LEDC PWM (非函数，为硬件寄存器层面)
```

### 2.1 Layer0 -- 硬件层（物理计算基底）

本层不是软件函数，而是固化在硬件中的物理计算。但以下软件函数直接与硬件层交互：

| 函数编号 | 函数名 | 与硬件层的关系 |
|----------|--------|---------------|
| F-MC-035 | `MotorController::init()` | 配置GPIO引脚模式、LEDC PWM通道、中断引脚绑定 |
| F-MC-005 | `MotorController::updateEncoders()` | 读取编码器中断计数器（int32_t硬件寄存器读取） |
| F-MC-004 | `MotorController::setMotorSpeed()` | 写入LEDC PWM寄存器、方向GPIO |
| F-SCL-003 | `SensorCalibration::getLeftRaw()` | 读取ADC1寄存器 |
| F-SCL-008 | `SensorCalibration::readNoise()` | 读取悬空GPIO35 ADC值（热噪声物理随机源） |

*注: 编码器IRAM_ATTR中断服务程序(encLeftISR/encRightISR)为硬件层直接代码，运行在RAM中，不依赖Flash。*

### 2.2 Layer1 -- 驱动层（传感器/执行器的直接封装）

| 函数编号 | 函数名 | 职责说明 |
|----------|--------|---------|
| F-MC-004 | `MotorController::setMotorSpeed()` ★ | PWM死区补偿、分方向四通道增益补偿、方向控制 |
| F-MC-005 | `MotorController::updateEncoders()` ★ | 编码器正交解码、有符号int32_t距离累计 |
| F-MC-035 | `MotorController::init()` | GPIO/LEDC/中断/ADC初始化 |
| F-MC-036 | `MotorController::stopMotors()` | PWM归零 |
| F-MC-037 | `MotorController::enableMotor()` | 解除电机锁定 |
| F-MC-038 | `MotorController::disableMotor()` | 电机安全锁定 |
| F-MC-039 | `MotorController::isMotorEnabled()` | 锁定状态查询 |
| F-MC-028 | `MotorController::getLeftTicks()` | 左编码器脉冲查询 |
| F-MC-029 | `MotorController::getRightTicks()` | 右编码器脉冲查询 |
| F-MC-030 | `MotorController::getEncoderDiff()` | 左右编码器差值 |
| F-MC-031 | `MotorController::getDistanceTicks()` | 总距离查询 |
| F-MC-032 | `MotorController::resetDistanceTicks()` | 距离归零 |
| F-MC-033 | `MotorController::getLeftSensor()` | 左传感器读数 |
| F-MC-034 | `MotorController::getRightSensor()` | 右传感器读数 |
| F-MC-014 | `MotorController::getObstacleThreshold()` | 障碍阈值查询 |
| F-MC-015 | `MotorController::getClearThreshold()` | 畅通阈值查询 |
| F-SCL-001 | `SensorCalibration::calibrate()` | 零点校准（读取无遮挡基准值并减去） |
| F-SCL-002 | `SensorCalibration::setThresholds()` | 阈值设置 |
| F-SCL-003~F-SCL-008 | SensorCalibration所有getter | 传感器原始数据存取 |

### 2.3 Layer2 -- 缓存层/具身算法层（物理结构即算法的核心执行层）

本层是"物理结构即算法"最直接的代码体现，每10ms一帧：

| 函数编号 | 函数名 | 职责说明 |
|----------|--------|---------|
| F-MC-049 | `MotorController::update()` ★ | 主更新循环：基因驱动行为表评估→动作执行→卡死检测→混沌触发→帧日志 |
| F-MC-007 | `MotorController::setMotorState()` ★ | 3bit状态机（IDLE/FORWARD/TURN/REVERSE/CHAOS/DEAD） |
| F-MC-008 | `MotorController::detectStuckWithWindow()` ★ | 滑动窗口物理终止检测（连续10帧编码器无变化=堵转） |
| F-MC-040 | `MotorController::startPhysicsAction()` ★ | 非阻塞物理动作启动（脉冲式控制） |
| F-MC-041 | `MotorController::updatePhysicsAction()` ★ | 物理终止条件检查（堵转检测+3秒安全兜底） |
| F-MC-042 | `MotorController::forceStopAction()` | 强制停止动作 |
| F-MC-043 | `MotorController::isActionInProgress()` | 动作状态查询 |
| F-MC-044 | `MotorController::startChaos()` ★ | 混沌模式启动：激活噪声驱动、记录起始快照 |
| F-MC-045 | `MotorController::updateChaos()` ★ | 混沌模式更新：噪声连续调制(0.7-1.3)PWM、快照收集、恢复检测 |
| F-MC-046 | `MotorController::endChaos()` ★ | 混沌结束：记录终止距离、冲刷快照、恢复状态机 |
| F-MC-047 | `MotorController::startFrameLog()` | 帧日志会话初始化 |
| F-MC-048 | `MotorController::logFrame()` | 逐帧数据记录（传感器/PWM/方向/混沌标志） |
| F-MC-050 | `MotorController::getFrameLog()` | 帧日志引用查询 |
| F-MC-009 | `MotorController::isStuck()` | 卡死状态查询 |
| F-MC-010 | `MotorController::isChaosActive()` | 混沌状态查询 |
| F-MC-011 | `MotorController::isDead()` | 死亡标志查询 |
| F-MC-012 | `MotorController::getMotorState()` | 状态机查询 |
| F-MC-013 | `MotorController::getStateDuration()` | 状态持续时间 |
| F-MC-020 | `MotorController::getTotalStuckTime()` | 累计卡死时间 |
| F-MC-021 | `MotorController::getStateDistanceDelta()` | 状态内距离增量 |
| F-MC-022 | `MotorController::getChaosDuration()` | 混沌持续时间 |
| F-MC-027 | `MotorController::resetStuck()` | 卡死状态重置 |
| F-MC-006 | `MotorController::clearStuckState()` | 堵转标志清除 |
| F-MC-016 | `MotorController::getChaosNoiseAmplifier()` | 混沌噪声放大系数 |
| F-MC-017 | `MotorController::getChaosMinPwm()` | 混沌最小PWM |
| F-MC-018 | `MotorController::getChaosTimeoutMs()` | 混沌超时阈值 |
| F-RLB-001 | `RAMLogBuffer::compressFrame()` | 差分编码压缩 |
| F-RLB-002 | `RAMLogBuffer::init()` | 6000帧环形缓冲初始化 |
| F-RLB-003 | `RAMLogBuffer::startNewTest()` | 新测试会话 |
| F-RLB-004 | `RAMLogBuffer::addFrame()` ★ | 帧数据追加到环形缓冲 |
| F-RLB-007~F-RLB-011 | RAMLogBuffer查询函数 | 缓冲区状态查询 |
| F-GLB-003 | `loop()` ★ | Arduino主循环（10ms帧调度+串口命令+进化推进） |

### 2.4 Layer3 -- 持久层/实验基础设施层（数据持久化与完整性保障）

| 函数编号 | 函数名 | 职责说明 |
|----------|--------|---------|
| F-FLU-001~F-FLU-005 | FileUtils全部函数 | 原子写入、安全读取、文件操作 |
| F-ROB-001~F-ROB-022 | RobustStorage全部函数 | 进化历史与混沌记录持久化 |
| F-ROL-001~F-ROL-017 | RollingStorage全部函数 | 滚动存储管理、帧日志持久化 |
| F-GST-001~F-GST-019 | GeneStorage全部函数 | 基因存储、实验生命周期管理 |
| F-MC-001~F-MC-003 | 混沌快照持久化函数 | 混沌快照的缓冲与SPIFFS写入 |
| F-MC-024, F-MC-025 | 混沌快照查询函数 | 快照数据查询 |

### 2.5 Layer4 -- 业务层/元算法层（进化逻辑与新奇度搜索）

| 函数编号 | 函数名 | 职责说明 |
|----------|--------|---------|
| F-EVE-001~F-EVE-025 | EvolutionEngine全部函数 | 进化引擎：种群管理、选择、交叉、变异、混沌规则反推 |
| F-NOV-001~F-NOV-006 | NoveltyArchive全部函数 | 新奇度计算、档案管理、行为描述符提取 |
| F-GNE-001~F-GNE-004 | Gene全部函数 | 基因的init/mutate/crossover/evaluateCondition |
| F-BHR-001~F-BHR-003 | BehaviorRule全部函数 | 行为规则的随机化/约束/比较 |
| F-BHD-001~F-BHD-003 | BehaviorDescriptor全部函数 | 行为描述符的初始化/距离计算/归一化 |
| F-MC-019 | `MotorController::syncGeneParams()` ★ | 基因参数同步到电机控制器（11个物理参数） |
| F-FVM-001~F-FVM-003 | FirmwareVersionManager全部函数 | 固件版本感知的数据清理 |

### 2.6 Layer5 -- 接口层（Web服务与数据通信）

| 函数编号 | 函数名 | 职责说明 |
|----------|--------|---------|
| F-CWS-001~F-CWS-016 | CarWebServer全部函数 | Web服务器：管理页面、API路由、数据下载 |
| F-CSW-001~F-CSW-003 | ChunkedStreamWriter全部函数 | HTTP chunked流式文件传输 |
| F-GLB-001 | `initWiFi()` | WiFi AP初始化 |
| F-GLB-004~F-GLB-011 | 串口命令函数 | 离线审计工具：文件列表、状态打印、数据解析 |
| F-MC-023, F-MC-026 | 混沌统计函数 | 混沌测试统计数据的查询与重置 |

### 2.7 Layer6 -- 工具层（日志、入口、校验）

| 函数编号 | 函数名 | 职责说明 |
|----------|--------|---------|
| F-LOG-001~F-LOG-006 | Logger全部函数 | 日志输出系统 |
| F-CRC-001~F-CRC-003 | CRC32全部函数 | 数据完整性校验 |
| F-GLB-002 | `setup()` | Arduino系统初始化（所有模块统一初始化入口） |

---

## 三、核心调用依赖关系

### 3.1 四大核心链路

系统运行在四条交织的功能链路上。以下详述每条链路的关键函数调用关系（A→B表示A调用B）。

#### 3.1.1 链路一：行动线（Action Line）-- 传感器→行为评估→电机执行→物理终止

```
F-GLB-003 loop()
  └→ F-MC-049 MotorController::update() ★
      ├→ F-SCL-003/004 getLeftRaw()/getRightRaw()  [读取传感器原始ADC值]
      ├→ F-SCL-008 readNoise() ★                    [读取物理噪声]
      ├→ F-SCL-005/006/007 isObstacleLeft/Right/Clear() [障碍判断]
      ├→ F-MC-043 isActionInProgress()               [检查是否有活跃动作]
      │   ├─ [若无活跃动作] →
      │   │   F-GNE-002 Gene::evaluateCondition() ★  [评估行为表规则]
      │   │   └→ F-BHR-003 BehaviorRule::compare()   [规则条件比较]
      │   │   └→ F-MC-040 startPhysicsAction() ★     [匹配后开始物理动作]
      │   │       └→ F-MC-004 setMotorSpeed() ★      [PWM死区补偿+增益+方向控制]
      │   │           └→ [ledcWrite()硬件PWM寄存器]
      │   └─ [若有活跃动作] →
      │       F-MC-041 updatePhysicsAction() ★       [物理终止检测]
      │       ├→ F-MC-005 updateEncoders() ★          [读取编码器脉冲]
      │       ├→ [堵转检测: 连续10帧编码器无变化=物理停止]
      │       └→ [安全兜底: 3秒超时自动终止]
      ├→ F-MC-008 detectStuckWithWindow() ★          [滑动窗口卡死检测]
      ├→ F-MC-007 setMotorState() ★                  [状态机转换]
      │   └→ F-MC-046 endChaos() ★                   [死亡时触发混沌结束]
      ├→ [若卡死且混沌未激活] →
      │   F-MC-044 startChaos() ★                    [混沌模式启动]
      │   └→ F-MC-045 updateChaos() ★                [噪声连续调制PWM]
      │       └→ F-MC-004 setMotorSpeed() ★           [物理噪声→PWM直接映射]
      └→ F-MC-048 logFrame() ★                       [每帧数据记录]
          └→ F-RLB-004 RAMLogBuffer::addFrame() ★     [写入RAM环形缓冲]
```

**行动线核心特征**: 这是"脉冲式控制"的直接体现：`startPhysicsAction()`一次设置电机目标PWM（无逐帧平滑），`updatePhysicsAction()`每帧仅检测物理停止。速度变化曲线由电机电感、齿轮箱惯性、轮胎摩擦力自然完成，软件完全不参与平滑控制。

#### 3.1.2 链路二：记录线（Record Line）-- 帧日志→行为描述符→历史持久化

```
F-EVE-003 EvolutionEngine::endCurrentTestImpl() ★
  ├→ F-MC-050 getFrameLog()                          [获取帧日志缓冲区]
  ├→ F-NOV-005 NoveltyArchive::extractFromFrameLog() ★ [从帧日志提取12维行为描述符]
  │   └→ F-BHD-001 BehaviorDescriptor::init()
  │   └→ F-NOV-001 updateMaxValues()                  [更新档案最大值]
  ├→ F-NOV-003 computeNovelty() ★                     [计算新奇度得分(k=5近邻)]
  │   └→ F-BHD-002 BehaviorDescriptor::distance()
  │   └→ F-BHD-003 BehaviorDescriptor::normalize()
  ├→ F-NOV-004 addIfNovel() ★                        [新奇度高则加入档案]
  ├→ F-GST-014 saveIndividualRecord()                 [保存个体基因CSV记录]
  │   └→ F-FLU-002 FileUtils::atomicWriteString()
  ├→ F-ROB-004 RobustStorage::addRecord()             [添加进化历史记录]
  │   └→ F-ROB-009 tick()                             [tick调度可能触发flush]
  │       └→ F-ROB-006 flushBuffer()
  │           └→ F-FLU-001 FileUtils::atomicWrite()
  ├→ [若有混沌触发] →
  │   F-ROB-005 addChaosRecord()                      [添加混沌测试记录]
  │   └→ F-GST-015 saveChaosRecord()                  [混沌CSV记录持久化]
  └→ F-ROL-014 RollingStorage::incrementalSave()      [增量保存帧日志(每500帧)]
      └→ F-ROL-013 saveFrameLog()                     [二进制格式帧日志写入SPIFFS]
          ├→ F-CRC-002 CRC32::calculate()              [文件头CRC32校验]
          └→ F-FLU-001 FileUtils::atomicWrite()        [原子写入]
```

**记录线核心特征**: 这是数据完整性保障链：帧日志经RAM缓冲→行为描述符提取→新奇度计算→多类型记录持久化→CRC32校验→原子写入。全链路数据可追溯，任何第三方可仅凭SPIFFS文件重现完整实验过程。

#### 3.1.3 链路三：存储线（Storage Line）-- SPIFFS空间管理→滚动覆盖→实验生命周期

```
系统启动时:
F-GLB-002 setup()
  ├→ F-FVM-001 FirmwareVersionManager::isNewVersion() [版本检测]
  │   └→ [若是新版本] F-FVM-002 cleanAllData()        [清理旧格式数据]
  ├→ F-ROB-002 RobustStorage::init()
  │   ├→ F-ROB-001 loadHistory() (private)            [从SPIFFS加载历史记录]
  │   └→ [SPIFFS挂载失败→降级为纯RAM模式]
  ├→ F-ROL-011 RollingStorage::init()
  │   ├→ F-ROL-009 ensureChipID()                     [设备ID管理]
  │   └→ F-ROL-002 getStoredGenerations() (private)    [加载已有代数]
  └→ F-GST-001 GeneStorage::init()
      ├→ F-GST-019 loadExperimentState() (private)     [加载实验状态标记]
      ├→ [若实验未完成→恢复进度]
      └→ [若实验已完成或新固件→F-GST-003 startNewExperiment()]

运行时空间管理:
F-ROL-012 RollingStorage::ensureSpace()
  ├→ F-ROL-001 getFreeSpace() (private)               [查询剩余空间]
  └→ [若空间不足50KB] → F-ROL-004 deleteOldestGeneration() [删除最旧代]
      └→ F-ROL-003 deleteGeneration() (private)         [逐文件删除]

实验生命周期:
F-EVE-004 EvolutionEngine::nextIndividualImpl() ★
  ├→ [种群所有个体完成测试] →
  │   F-EVE-006 evolveImpl() ★                         [代际进化]
  │   └→ F-GST-012 savePopulation()                    [种群快照持久化]
  │       └→ F-CRC-002 CRC32::calculate()
  │       └→ F-FLU-001 FileUtils::atomicWrite()
  └→ F-GST-011 GeneStorage::incrementGeneration()      [代数+1]
      └→ F-GST-018 saveExperimentState() (private)      [状态标记持久化]
```

**存储线核心特征**: SPIFFS+RAM双层架构。帧数据在RAM中积累（6000帧环形缓冲），每500帧触发增量保存，个体死亡时全量保存。原子写入(临时文件+重命名)确保掉电不丢数据。CRC32校验确保数据未被篡改。5代滚动覆盖+50KB阈值自动删除最旧代。

#### 3.1.4 链路四：进化线（Evolution Line）-- 种群管理→选择→交叉→变异→混沌规则注入

```
F-EVE-002 EvolutionEngine::startCurrentTestImpl() ★
  ├→ F-MC-019 MotorController::syncGeneParams() ★     [基因参数→电机控制器(11物理参数)]
  └→ F-MC-047 startFrameLog()                          [开始帧日志会话]

[个体测试30秒，loop()持续调用MotorController::update()]

F-EVE-003 EvolutionEngine::endCurrentTestImpl() ★
  ├→ [记录线操作: 行为描述符提取→新奇度计算→记录保存] (见链路二)
  ├→ [若混沌触发] →
  │   F-MC-025 getChaosSnapshots()                     [获取混沌快照]
  │   └→ F-EVE-005 inferRuleFromSnapshot() ★           [从快照反推行为规则]
  │       └→ F-GNE-003 Gene::mutate() ★                [物理经验→基因编码]
  └→ F-EVE-004 nextIndividualImpl() ★
      └→ [种群完成→触发进化] →
          F-EVE-006 evolveImpl() ★
          ├→ [按新奇度降序排序种群]
          ├→ [精英保留: 前25%(4个体)直接进入下一代]
          ├→ F-EVE-008 getAvgNovelty()                  [计算种群平均新奇度]
          ├→ F-EVE-007 tournamentSelectByNovelty()      [锦标赛选择(k=3)]
          │   └→ F-NOV-003 computeNovelty()              [新奇度得分查询]
          ├→ F-GNE-004 Gene::crossover() ★              [规则子集交叉]
          │   └→ [父1前cut1条 + 父2后cut2条 → 子代]
          └→ F-GNE-003 Gene::mutate() ★                 [物理噪声驱动变异]
              └→ F-SCL-008 SensorCalibration::readNoise() ★ [悬空ADC热噪声]
              └→ F-EVE-023 setMutationRate()            [动态调整变异率: 0.05-0.50]

[代际存档]
F-GST-012 GeneStorage::savePopulation()                [种群快照二进制格式]
F-GST-014 saveIndividualRecord()                       [个体记录CSV格式]
F-GST-015 saveChaosRecord()                            [混沌记录CSV格式]
```

**进化线核心特征**: 这是"代码退场→算法退场→进化退场"三层递归的直接体现。
- **代码退场**: 混沌期间`updateChaos()`不调用`evaluateCondition()`，物理噪声直接驱动PWM，代码不决策。
- **算法退场**: 11个物理参数从硬编码变为基因编码（`syncGeneParams()`同步），不预设脱困策略。
- **进化退场**: 混沌行为被编码为基因（`inferRuleFromSnapshot()`），通过交叉遗传进入下一代，物理涌现→基因编码→遗传进化形成闭环。

### 3.2 依赖矩阵（关键模块间调用关系）

| 调用者\被调用者 | MC | EVE | NOV | GST | ROL | ROB | RLB | GNE | SCL | CRC | FLU | FVM | LOG | CSW | CWS | GLB |
|------------------|-----|-----|-----|-----|-----|-----|-----|-----|-----|-----|-----|-----|-----|-----|-----|-----|
| **MC** (MotorController) | - | | | | F-ROL-014 | | F-RLB-004 | F-GNE-002 | F-SCL-003~008 | | | | | | | |
| **EVE** (EvolutionEngine) | F-MC-019 F-MC-025 F-MC-047 F-MC-050 | - | F-NOV-003 F-NOV-004 F-NOV-005 | F-GST-012 F-GST-014 F-GST-015 | | F-ROB-004 F-ROB-005 | | F-GNE-003 F-GNE-004 | | | | | | | | |
| **NOV** (NoveltyArchive) | | | - | | | | | | | | | | | | | |
| **GST** (GeneStorage) | | | | - | | | | | | F-CRC-002 | F-FLU-001 F-FLU-002 | | | | | |
| **ROL** (RollingStorage) | | | | | - | | | | | F-CRC-002 | F-FLU-001 | | | | | |
| **ROB** (RobustStorage) | | | | | | - | | | | | F-FLU-001 | | | | | |
| **RLB** (RAMLogBuffer) | | | | | | | - | | | | | | | | |
| **GNE** (Gene) | | | | | | | | - | F-SCL-008 | | | | | | |
| **CWS** (CarWebServer) | F-MC-037 F-MC-038 | F-EVE-005 F-EVE-009 | | F-GST-013 F-GST-017 | F-ROL-005 F-ROL-008 | F-ROB-013 F-ROB-014 | | | | | | | | F-CSW-001~003 | - | |
| **GLB** (全局) | | F-EVE-009 F-EVE-012 | | F-GST-001 F-GST-017 | F-ROL-008 F-ROL-011 | F-ROB-002 | F-RLB-005 | | | | | F-FVM-001 | | | | - |

*说明: MC=MotorController, EVE=EvolutionEngine, NOV=NoveltyArchive, GST=GeneStorage, ROL=RollingStorage, ROB=RobustStorage, RLB=RAMLogBuffer, GNE=Gene, SCL=SensorCalibration, CRC=CRC32, FLU=FileUtils, FVM=FirmwareVersionManager, LOG=Logger, CSW=ChunkedStreamWriter, CWS=CarWebServer, GLB=全局函数*

### 3.3 关键依赖说明

1. **EvolutionEngine → MotorController**: 这是最关键的跨层依赖。`endCurrentTestImpl()`通过`getFrameLog()`获取物理层的帧日志，通过`syncGeneParams()`将元算法层的基因编码下发到具身算法层。这是"物理↔算法"双向通道的核心。

2. **loop() → MotorController::update()**: Arduino主循环每次迭代都调用`update()`，构成10ms帧循环的核心执行路径。这是系统唯一的时间驱动点（除硬件中断外）。

3. **MotorController → RAMLogBuffer**: `logFrame()`→`addFrame()`是行动线通往记录线的桥梁。每帧传感器+电机数据经由这条路径进入6000帧环形缓冲。

4. **EvolutionEngine → NoveltyArchive**: 代际进化时`evolveImpl()`和`tournamentSelectByNovelty()`依赖`computeNovelty()`评估每个个体的行为新颖性。这是"新奇度驱动选择"的核心依赖。

5. **GeneStorage/RollingStorage/RobustStorage → FileUtils + CRC32**: 所有持久化操作都通过`atomicWrite()`+`calculate()`确保数据完整性。这是实验可复现性的工程保障。

6. **CarWebServer → ChunkedStreamWriter**: Web下载接口通过流式传输逐块发送SPIFFS文件。这是避免大文件下载阻塞主循环的架构设计。

---

## 四、"物理结构即算法"核心函数标注

以下函数是"物理结构即算法"理念在代码层面的直接体现。它们共同构成了"去标定、去滤波、去PID"三原则和"脉冲式控制、噪声连续调制、物理终止条件"三大物理增强控制的完整实现。

### 4.1 物理增强控制三大机制

| 机制 | 核心函数 | 编号 | 物理化原理 |
|------|----------|------|-----------|
| **脉冲式控制** | `MotorController::startPhysicsAction()` | F-MC-040 | 一次性设置目标PWM（无逐帧平滑），速度变化由电机电感/齿轮箱惯性/轮胎摩擦力自然完成 |
| **脉冲式控制** | `MotorController::updatePhysicsAction()` | F-MC-041 | 每帧仅检测编码器是否停止（物理状态查询），不参与速度调节 |
| **物理终止条件** | `MotorController::detectStuckWithWindow()` | F-MC-008 | 连续10帧编码器无脉冲变化=物理上已停止，动作时长由物理系统决定 |
| **物理终止条件** | `MotorController::update()` | F-MC-049 | 主循环中物理终止与卡死检测的调度入口 |
| **噪声连续调制** | `SensorCalibration::readNoise()` | F-SCL-008 | 读取悬空GPIO35 ADC热噪声，半导体载流子热运动产生的本质不确定性 |
| **噪声连续调制** | `MotorController::updateChaos()` | F-MC-045 | 每帧以0.7~1.3调制因子连续扰动所有电机PWM值 |
| **噪声连续调制** | `Gene::mutate()` | F-GNE-003 | 变异步长由物理噪声实时采样值经线性变换决定，非PRNG |
| **噪声连续调制** | `Gene::crossover()` | F-GNE-004 | 规则子集交叉操作，变异幅度由物理噪声决定 |

### 4.2 行为表规则引擎

| 核心函数 | 编号 | 物理化原理 |
|----------|------|-----------|
| `Gene::evaluateCondition()` | F-GNE-002 | IF-THEN规则评估：传感器原始ADC值直接与阈值比较（>/</==），不标定、不滤波 |
| `BehaviorRule::compare()` | F-BHR-003 | 单条规则的条件匹配：基于原始物理量（ADC值、脉冲数、毫秒）的无语义比较 |
| `BehaviorRule::randomize()` | F-BHR-001 | 规则随机生成：条件类型、阈值、电机指令均从物理噪声驱动的随机源产生 |

### 4.3 状态机与混沌机制

| 核心函数 | 编号 | 物理化原理 |
|----------|------|-----------|
| `MotorController::setMotorState()` | F-MC-007 | 3bit状态机编码：状态转换由物理条件（卡死/碰撞/恢复）驱动，非代码逻辑预设 |
| `MotorController::startChaos()` | F-MC-044 | 混沌启动：卡死时物理结构"声称"了对混沌的需求，代码仅是"麦克风" |
| `MotorController::endChaos()` | F-MC-046 | 混沌结束：记录物理边界（终止距离）以便自身对照差分分析 |
| `MotorController::syncGeneParams()` | F-MC-019 | 11个物理参数从基因编码同步到控制器（分方向增益、死区阈值、混沌强度等） |
| `EvolutionEngine::inferRuleFromSnapshot()` | F-EVE-005 | 混沌快照反推规则：物理经验→基因编码，实现"进化退场"：涌现行为→遗传编码 |
| `EvolutionEngine::startCurrentTestImpl()` | F-EVE-002 | 测试开始时的基因→物理同步 |
| `EvolutionEngine::endCurrentTestImpl()` | F-EVE-003 | 测试结束时的物理→基因反馈（新奇度评估+混沌规则注入） |

### 4.4 物理层直接交互

| 核心函数 | 编号 | 物理化原理 |
|----------|------|-----------|
| `MotorController::setMotorSpeed()` | F-MC-004 | PWM死区补偿+分方向四通道增益：硬件不对称被封装而非消除，成为"进化的素材" |
| `MotorController::updateEncoders()` | F-MC-005 | 编码器正交解码：物理旋转直接转化为有符号int32_t脉冲计数，不经过运动学模型 |
| `loop()` | F-GLB-003 | 10ms帧循环：系统唯一的时间驱动，所有时间都来自硬件定时器和编码器脉冲 |

### 4.5 数据管线（实验基础设施的物理保障）

| 核心函数 | 编号 | 物理化原理 |
|----------|------|-----------|
| `MotorController::logFrame()` | F-MC-048 | 逐帧记录：传感器原始ADC + 电机原始PWM + 方向位 + chaosActive标志，不经过任何标定或转换 |
| `RAMLogBuffer::addFrame()` | F-RLB-004 | 差分编码压缩后写入环形缓冲，保持原始物理量的保真度 |
| `NoveltyArchive::extractFromFrameLog()` | F-NOV-005 | 从原始帧日志提取12维行为描述符：传感器/运动/策略三组维度，全部基于物理观察量 |
| `NoveltyArchive::computeNovelty()` | F-NOV-003 | k近邻新奇度计算：行为多样性的数学表征，环境选择压在状态空间中留下的"足迹" |
| `NoveltyArchive::addIfNovel()` | F-NOV-004 | 新奇度阈值判断：决定行为描述符是否加入档案，档案是"物理结构在当前约束下可能性空间的展开程度"的指标 |
| `RobustStorage::tick()` | F-ROB-009 | 周期性保存调度：10秒或满5条触发刷新，平衡Flash磨损与数据完整性 |
| `EvolutionEngine::evolveImpl()` | F-EVE-006 | 代际进化：精英保留+锦标赛选择+交叉+变异，在行为空间中搜索而非在参数空间中优化 |

### 4.6 "物理结构即算法"的总函数统计

以上标注的核心函数共计 **28个**（带★标记的函数 + loop()），分布在以下模块：

| 模块 | 核心函数数 | 核心函数列表 |
|------|-----------|-------------|
| MotorController | 14 | F-MC-004, F-MC-005, F-MC-007, F-MC-008, F-MC-019, F-MC-040, F-MC-041, F-MC-044, F-MC-045, F-MC-046, F-MC-048, F-MC-049 |
| EvolutionEngine | 5 | F-EVE-002, F-EVE-003, F-EVE-004, F-EVE-005, F-EVE-006 |
| Gene | 3 | F-GNE-002, F-GNE-003, F-GNE-004 |
| NoveltyArchive | 3 | F-NOV-003, F-NOV-004, F-NOV-005 |
| SensorCalibration | 1 | F-SCL-008 |
| RAMLogBuffer | 1 | F-RLB-004 |
| RobustStorage | 1 | F-ROB-009 |
| 全局函数 | 1 | F-GLB-003 |

### 4.7 核心函数的物理语义总结

上述28个核心函数共同构成了"物理结构即算法"在代码层面的完整实现：

- **不会计算的行为**：`setMotorSpeed()`不执行PID、`evaluateCondition()`不进行标定、`readNoise()`不滤波--三去原则的代码化
- **由物理决定的时间**：`detectStuckWithWindow()`和`updatePhysicsAction()`中的动作时长取决于电机扭矩与地面摩擦力--物理结构在"计时"
- **由物理驱动的变异**：`mutate()`的变异步长来自悬空ADC热噪声--半导体载流子的热运动在"决定"变异方向和幅度
- **由物理驱动的探索**：`updateChaos()`中的PWM调制来自GPIO35实时读数--物理噪声在"创造"新行为
- **由物理定义的状态**：`setMotorState()`的状态转换由卡死/碰撞条件触发--物理结构在"决定"状态机的走向
- **由物理塑造的选择**：`computeNovelty()`和`evolveImpl()`中的新奇度得分来自实际行为数据--环境选择压在"书写"适应性景观

这28个函数不是"实现算法"的函数，而是"让物理结构运行"的函数。它们的共同特征是：程序代码的作用不是"创造"响应，而是"激活"物理结构中封装的响应模式。

---

## 附录 A：模块缩写对照表

| 缩写 | 模块全称 | 函数数量 | 层归属 |
|------|----------|----------|--------|
| LOG | Logger | 6 | L6 |
| CRC | CRC32 | 3 | L6 |
| FVM | FirmwareVersionManager | 3 | L4 |
| FLU | FileUtils | 5 | L3 |
| ROB | RobustStorage | 22 | L3 |
| ROL | RollingStorage | 17 | L3 |
| GST | GeneStorage | 19 | L3 |
| NOV | NoveltyArchive | 6 | L4 |
| RLB | RAMLogBuffer | 11 | L2 |
| MC | MotorController | 50 | L1/L2/L3 |
| EVE | EvolutionEngine | 25 | L4 |
| CSW | ChunkedStreamWriter | 3 | L5 |
| CWS | CarWebServer | 16 | L5 |
| GLB | 全局函数 | 11 | L2/L5/L6 |
| GNE | Gene结构体 | 4 | L4 |
| BHR | BehaviorRule结构体 | 3 | L4 |
| BHD | BehaviorDescriptor结构体 | 3 | L4 |
| SCL | SensorCalibration | 8 | L1 |

**总计: 约 220 个函数/方法**（含BehaviorRule/BehaviorDescriptor/SensorCalibration等补充模块的函数）。

## 附录 B：函数编号总览（按编号排序）

```
F-LOG-001~006: Logger (6)
F-CRC-001~003: CRC32 (3)
F-FVM-001~003: FirmwareVersionManager (3)
F-FLU-001~005: FileUtils (5)
F-ROB-001~022: RobustStorage (22)
F-GNE-001~004: Gene (4)
F-BHR-001~003: BehaviorRule (3)
F-BHD-001~003: BehaviorDescriptor (3)
F-SCL-001~008: SensorCalibration (8)
F-MC-001~050: MotorController (50)
F-ROL-001~017: RollingStorage (17)
F-GST-001~019: GeneStorage (19)
F-NOV-001~006: NoveltyArchive (6)
F-RLB-001~011: RAMLogBuffer (11)
F-EVE-001~025: EvolutionEngine (25)
F-CSW-001~003: ChunkedStreamWriter (3)
F-CWS-001~016: CarWebServer (16)
F-GLB-001~011: 全局函数 (11)
```

---

*文档生成完毕。共包含 18 个模块/结构体，约 220 个函数的完整编号索引、层次归类、四大链路依赖分析和"物理结构即算法"核心函数标注。*